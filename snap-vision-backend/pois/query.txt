Go to: https://overpass-turbo.eu/

run the below query, and export as geoJSON to get University of Pretoria POI's:

[out:json][timeout:25];

// Get the UP campus relation
rel["name"="University of Pretoria"]["type"="multipolygon"];
map_to_area->.upCampus;

// Now get all buildings inside that area
(
  way["building"](area.upCampus);
  relation["building"](area.upCampus);
);
out body;
>;
out skel qt;